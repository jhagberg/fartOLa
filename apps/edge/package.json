{
  "name": "@fartola/edge",
  "version": "0.1.0",
  "description": "fartOLa — single-laptop orienteering training MVP (edge bridge + SQLite event log + SPA + SportIdent bridge). Installs the `fartola` binary via `npm install -g`.",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "main": "./dist/server.cjs",
  "exports": {
    ".": {
      "types": "./dist/server.d.ts",
      "import": "./dist/server.mjs",
      "require": "./dist/server.cjs"
    }
  },
  "bin": {
    "fartola": "./dist/bin/fartola.cjs"
  },
  "scripts": {
    "build": "tsup && mkdir -p dist/xml && cp src/xml/IOF.xsd dist/xml/IOF.xsd",
    "dev": "tsx watch src/bin/fartola.ts",
    "test": "node --test --test-reporter=spec --import tsx 'src/**/*.test.ts' 'scripts/**/*.test.ts'",
    "test:watch": "node --test --watch --test-reporter=spec --import tsx 'src/**/*.test.ts' 'scripts/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "db:generate": "drizzle-kit generate",
    "pack:tarball": "bash scripts/build-tarball.sh"
  },
  "files": [
    "dist",
    "drizzle",
    "systemd",
    "udev",
    "README.md",
    "NOTICE.md"
  ],
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/multipart": "^10.1.0",
    "@fastify/sensible": "^6.0.5",
    "@fastify/static": "^9.1.3",
    "@fastify/websocket": "^11.3.0",
    "better-sqlite3": "^12.10.0",
    "drizzle-orm": "^0.45.2",
    "fast-xml-parser": "^5.10.1",
    "fastify": "^5.12.0",
    "fastify-plugin": "^5.1.0",
    "node-thermal-printer": "^4.4.3",
    "saxes": "6.0.0",
    "serialport": "^13.0.0",
    "sharp": "^0.35.3",
    "xmllint-wasm": "^5.3.0",
    "yauzl": "^3.4.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@fartola/shared-types": "workspace:*",
    "@fartola/sportident": "workspace:*",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "@types/yauzl": "^3.4.0",
    "drizzle-kit": "^0.31.10",
    "tsup": "^8.3.5",
    "tsx": "^4.23.12",
    "typescript": "^5.6.3",
    "ws": "^8.21.3"
  }
}
